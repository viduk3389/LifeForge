# Copy to .env.local (same folder as package.json) — Next.js does NOT load this file.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Use the anon/public key from Supabase → Project Settings → API (JWT format eyJ... or publishable sb_...)
